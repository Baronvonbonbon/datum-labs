# env/indexer.env  (cp from this) — on-chain indexer (run + serve share the db volume).
RPC_URL=https://eth-rpc-testnet.polkadot.io
DATUM_GOVERNANCE_ROUTER=0xAb22653cDcA7214636708721AeDAc289E8635e80
DB_PATH=/app/data/datum.db                # the indexer-data volume
PORT=4319
# START_BLOCK=auto                         # or a number to bound the initial backfill
